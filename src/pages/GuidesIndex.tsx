import { Link } from "react-router-dom";
import { useSiteData } from "../context/SiteDataContext";

export function GuidesIndex() {
  return (
    <main>
      // GuidesIndex page removed
    </main>
  );
}
