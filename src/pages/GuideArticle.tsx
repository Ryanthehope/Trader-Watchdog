import { Link, useParams } from "react-router-dom";
import { useSiteData } from "../context/SiteDataContext";

export function GuideArticle() {
  return (
    <main>
      // GuideArticle page removed
    </main>
  );
}
